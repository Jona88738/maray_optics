import { conn } from "../db/connectionMysql"

const getExpedientes = (req, res) =>{

    return res.json({})
}

const insertExpediente = (req, res) => {

    try {
        
    } catch (error) {
        
    }

    return res.json({})
}

export default {
    getExpedientes,
    insertExpediente
}