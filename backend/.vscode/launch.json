{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug con Nodemon",
      "runtimeExecutable": "nodemon",
      "program": "${workspaceFolder}/index.js", // Asegúrate que este archivo sea tu punto de entrada
      "restart": true,
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen",
      "env": {
        "NODE_ENV": "development"
      }
    }
  ]
}
